fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn==26.2.0          # prod process manager (Linux only — see Dockerfile CMD)
uvicorn-worker>=0.3.0     # gunicorn ASGI worker class; uvicorn.workers is deprecated
python-multipart>=0.0.9   # required by FastAPI for Form(...) / UploadFile
jinja2>=3.1.4             # Jinja2Templates — templates/index.html, eval.html, view.html
itsdangerous>=2.2.0       # signs the SessionMiddleware cookie
werkzeug>=3.1.8           # secure_filename() only — Starlette does not vendor it
httpx>=0.27.0             # fastapi.testclient.TestClient, used by test_eval_metrics.py
pymupdf>=1.27.2
python-docx>=1.1.2
filelock>=3.13.0

# Optional — only needed when VECTOR_BACKEND=qdrant.
# The app can run with VECTOR_BACKEND=memory without Qdrant.
qdrant-client>=1.19.0
